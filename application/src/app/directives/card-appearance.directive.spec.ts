import { CardAppearanceDirective } from './card-appearance.directive';

describe('CardAppearanceDirective', () => {
  it('should create an instance', () => {
    const directive = new CardAppearanceDirective();
    expect(directive).toBeTruthy();
  });
});
