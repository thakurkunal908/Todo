import { LimitViewContentPipe } from './limit-view-content.pipe';

describe('LimitViewContentPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitViewContentPipe();
    expect(pipe).toBeTruthy();
  });
});
